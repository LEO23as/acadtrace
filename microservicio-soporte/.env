# Servidor backend
PORT=5178

# Base de datos Supabase (Postgres) — mismo proyecto que el resto del SGA
DB_HOST=3.23.195.43
DB_PORT=5433
DB_NAME=sga
DB_USER=postgres
DB_PASSWORD=SgaProvU2026Db
DB_SSL=false

# JWT — MISMA secret que sga-principal (login único)
JWT_SECRET=sga-provincias-unidas-secret-key-2026-ecuador-uteq-sistemas

# URL del Spring Boot principal (solo lo usa el frontend en dev)
VITE_API_PRINCIPAL=http://localhost:8080/api

# Opcionales (tienen default razonable si se omiten)
# CORS_ORIGIN=*
# FRONTEND_DIST_PATH=dist
